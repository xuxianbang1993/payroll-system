# P1 Bugfix 待办清单

> **用途：** 新对话开取后，Claude 直接读取此文件即可恢复上下文并继续执行。
>
> **指令：** 请先阅读本文件，然后阅读 `plans/2026-02-16-p1-bugfix-plan.md` 获取完整任务细节，再继续从"下一步"开始执行。

**日期：** 2026-02-17（第二次更新）
**分支：** `bugfix/P1-current-issue`（基于 main `a06c565`）
**HEAD：** `19ef44b`

---

## 一、项目背景

- **项目：** 薪酬管理系统（Electron 40 + React 19 + TypeScript + Vite 7 + Zustand + better-sqlite3 + i18next）
- **仓库：** `https://github.com/xuxianbang1993/payroll-system`
- **工作目录：** `/Users/xuxianbang/Documents/payroll system/China/payroll-app`
- **开发策略文档：** `/Users/xuxianbang/Documents/payroll system/China/Devolop files SOP/薪酬系统-开发策略文档.md`（v3.6）
- **当前阶段：** P1 bugfix — 修复 code review 发现的全部问题后进入 P2

---

## 二、AI 模型分级策略（严格执行）

| 优先级 | 环境 | 模型 |
|--------|------|------|
| 1 | Codex | GPT-5.3-codex xhigh |
| 2 | Claude Code CLI - 简单任务 | ~~Haiku 4.5~~（API 不可用，用 **Sonnet** 替代，用户已批准） |
| 3 | Claude Code CLI - 中等任务 | **Sonnet 4.5 Thinking** |
| 4 | Claude Code CLI - 高难度任务 | **Opus 4.6** |

**规则：**
- 每个任务执行前必须告知用户当前使用的模型
- 必须经用户批准后才能开始开发
- Haiku 4.5 API 不可用（503 错误），所有原计划 Haiku 的任务改用 Sonnet
- Sonnet API 偶尔 503，若不可用可临时用 Opus 替代（需用户批准）

---

## 三、执行流程（subagent-driven development）

每个任务严格遵循以下流程：
1. **Implementer subagent** — 实现代码 + 自测 + commit
2. **Spec reviewer subagent** — 验证实现是否符合规格
3. **Code quality reviewer subagent** — 代码质量审查
4. 若审查发现问题 → implementer 修复 → 重新审查
5. 全部通过 → 标记完成 → 进入下一个任务

**技能依赖：** `superpowers:subagent-driven-development`

---

## 四、已完成的任务

### Batch A: Foundation Utilities ✅

| Task | 内容 | Commit | 状态 |
|------|------|--------|------|
| A1 | 提取共享工具函数 (format/error/type-guards) | `c11b55f` | ✅ 完成 |
| A2 | 提取 E2E 测试 helpers | `415642d` | ✅ 完成 |

**A1 创建的文件：**
- `src/utils/format.ts`（70 行）— formatAmount, formatBytes, formatCurrency, parseNumber, toNumber
- `src/utils/error.ts`（9 行）— toErrorMessage
- `src/utils/type-guards.ts`（24 行）— asObject, asRecord

**A2 创建的文件：**
- `tests/e2e/helpers.ts`（84 行）— launchElectronForTest, getFirstWindowOrThrow, getDbPath

### Batch B: i18n + Store Refactor ✅

| Task | 内容 | Commit | 状态 |
|------|------|--------|------|
| B1 | settings-store i18n + utils 提取 | `d32d486` | ✅ 完成 |
| B2 | employee-store i18n + utils 提取 | `fd6269a` | ✅ 完成 |
| B3 | OverviewPage i18n + 员工类型 enum | `149abf7` | ✅ 完成 |

**B1 创建的文件：**
- `src/utils/settings-utils.ts`（129 行）— normalizeSettings, computeOrgStats, normalizeCompanies, validateOrgName, validateCompanyShort
- `src/utils/i18n-utils.ts`（18 行）— resolveMessage（共享 i18n 消息解析工具）

**B2 创建的文件：**
- `src/utils/employee-utils.ts`（125→179 行，C1 新增 createEmptyForm, toFormModel, InlineEditDraft）

**B3 关键变更：**
- EmployeeType 从 `"管理"|"销售"` 改为 `"management"|"sales"`
- 三层向后兼容：employee-utils.ts、normalize.ts、sqlite-shared.ts
- OverviewPage KPI 标签全部 i18n 化
- 更新 23 个文件（含 10 个测试文件）

### Batch C: Page/Component Extraction ✅（本次会话完成）

| Task | 内容 | Commit | 审查结果 | 状态 |
|------|------|--------|----------|------|
| C1 | Split EmployeeListPage → EmployeeForm + EmployeeDetail + EmployeeInlineRow | `90b845d` | Spec ✅ Quality 9/10 ✅ | ✅ 完成 |
| C2 | Split ImportExportPage + p1-employee-import-export.ts | `83173ae` + `53c93a7`(fix) | Spec ✅ Quality ✅ (2 Important 已修复) | ✅ 完成 |
| C3 | Split BackupPage + sqlite-backup | `19ef44b` | Spec ✅ Quality ✅ (3 Minor 不阻塞) | ✅ 完成 |

**C1 创建的文件：**
- `src/components/EmployeeForm.tsx`（156 行）— 添加员工对话框
- `src/components/EmployeeDetail.tsx`（54 行）— 员工详情卡片
- `src/components/EmployeeInlineRow.tsx`（148 行）— 内联编辑行
- EmployeeListPage.tsx 从 595 行降至 190 行

**C2 创建的文件：**
- `src/lib/employee-import-parse.ts`（179 行）— 模板生成 + Excel 解析
- `src/lib/employee-import-merge.ts`（127 行）— 冲突检测 + 合并
- `src/components/ImportPreview.tsx`（98 行）— 导入预览
- `src/components/ExportPanel.tsx`（26 行）— 导出面板
- 删除了旧的 `p1-employee-import-export.ts`（353 行）
- ImportExportPage.tsx 从 339 行降至 198 行

**C3 创建的文件：**
- `src/components/BackupActions.tsx`（97 行）— 备份导出操作
- `src/components/RestoreActions.tsx`（160 行）— 恢复导入 + 清除操作
- `electron/db/repository/sqlite-import.ts`（125 行）— 导入备份逻辑
- BackupPage.tsx 从 244 行降至 85 行
- sqlite-backup.ts 从 246 行降至 146 行

**C2 修复记录：**
- `53c93a7` — 移除 `.ts` import 扩展名，消除重复的 `ensureCompanyFullName` 函数

### 架构决策（已锁定）

| 决策 | 选择 |
|------|------|
| i18n 策略 | Store 返回 key 字符串，组件层 `t()` 解析 |
| 参数化消息格式 | `"error.key\|param_value"`，用 `resolveMessage(msg, t)` 解析 |
| resolveMessage 位置 | `src/utils/i18n-utils.ts`（所有组件共用） |
| 员工类型 | `"management" \| "sales"`（向后兼容旧中文值） |
| replaceEmployees | 改为增量 CRUD（Task D1） |

---

## 五、待完成的任务

### Batch D: DB Layer — Incremental CRUD（Opus） ✅

| Task | 内容 | Commit | 审查结果 | 状态 |
|------|------|--------|----------|------|
| D1 | 添加 addEmployee/updateEmployee/deleteEmployee，废弃 replaceEmployees | 待 commit | 70/70 测试通过 | ✅ 完成 |

**D1 变更范围：** 11 个源文件 + 3 个测试文件
- `contracts.ts` — 新增 `addEmployee`/`updateEmployee`/`deleteEmployee` 接口 + `DeleteEmployeeResult` + `@deprecated replaceEmployees`
- `sqlite-employees.ts` — SQLite 原子 CRUD（事务保护，ID 自动生成）
- `legacy-adapter.ts` — Legacy JSON store 数组操作实现
- `switching-repository.ts` — 路由新方法通过 `writeByMode`
- `sqlite-adapter.ts` — 传递新方法
- `main.ts` — 3 个新 IPC handler（271 行，E1 会拆分）
- `preload.cts` — 暴露 3 个新 channel
- `electron-api.d.ts` — 类型声明
- `p1-repository.ts` — 3 个新 bridge wrapper + 提取 `invokeRepo<T>` helper（207→103 行）
- `employee-store.ts` — 用增量 API 替换全量 `replaceEmployees`
- 3 个测试文件更新适配新 API

### Batch E: IPC Architecture（Opus）

| Task | 内容 | 依赖 |
|------|------|------|
| **E1** | Split electron/main.ts（250 行）→ ipc/store-ipc.ts + db-ipc.ts + repository-ipc.ts + file-ipc.ts + index.ts + IPC 边界校验 | D1 |

### Batch F: Tests + Polish（Sonnet）

| Task | 内容 | 依赖 |
|------|------|------|
| **F1** | 新增组件测试：SocialConfig、Company、EmployeeForm | C1 ✅, E1 |
| **F2** | SocialConfigPage 示例计算改用 decimal.js | E1 |

### Batch G: Verification（Sonnet）

| Task | 内容 | 依赖 |
|------|------|------|
| **G1** | 全量回归 + 文件审计 + i18n 审计 + 证据刷新 | 全部 |

---

## 六、下一步操作

### 立即执行：E1（依赖已满足）

1. **E1**（Opus 4.6）— 拆分 main.ts（271 行）→ IPC 模块

**用户需批准：**
- E1 使用 Opus 4.6 模型

### 后续：
2. **F1, F2**（Sonnet）— 依赖 C1 ✅ 和 E1 完成
3. **G1**（Sonnet）— 全量回归验证

---

## 七、当前测试状态

- **Unit Tests：** 70/70 通过 ✅（2026-02-17 D1 完成后验证）
- **E2E Tests：** 上次手动验证 4/4 通过（Batch C 变更后尚未重新验证，需在 G1 验证）
- **Build：** C3 期间验证通过 ✅

---

## 八、关键文件速查

| 文件 | 用途 |
|------|------|
| `plans/2026-02-16-p1-bugfix-plan.md` | 完整实施计划（每个任务的详细步骤） |
| `plans/2026-02-16-p1-bugfix-design.md` | 设计文档（架构决策） |
| `China/Devolop files SOP/薪酬系统-开发策略文档.md` | 开发策略文档 v3.6（AI 模型分级、红线规则） |
| `src/utils/i18n-utils.ts` | 共享 resolveMessage 工具 |
| `src/utils/format.ts` | 共享格式化工具 |
| `src/utils/error.ts` | 共享错误处理工具 |
| `src/utils/type-guards.ts` | 共享类型守卫 |
| `src/utils/settings-utils.ts` | settings store 工具函数 |
| `src/utils/employee-utils.ts` | employee store 工具函数（179 行，含 createEmptyForm, toFormModel） |
| `tests/e2e/helpers.ts` | 共享 E2E 测试 helpers |
| `src/lib/employee-import-parse.ts` | 导入解析（模板+Excel 解析） |
| `src/lib/employee-import-merge.ts` | 导入合并（冲突检测+合并） |
| `src/components/EmployeeForm.tsx` | 添加员工对话框组件 |
| `src/components/EmployeeDetail.tsx` | 员工详情卡片组件 |
| `src/components/EmployeeInlineRow.tsx` | 内联编辑行组件 |
| `src/components/ImportPreview.tsx` | 导入预览组件 |
| `src/components/ExportPanel.tsx` | 导出面板组件 |
| `src/components/BackupActions.tsx` | 备份导出操作组件 |
| `src/components/RestoreActions.tsx` | 恢复/清除操作组件 |
| `electron/db/repository/sqlite-import.ts` | SQLite 导入备份逻辑 |

---

## 九、Git 提交历史（bugfix 分支）

```
19ef44b refactor(data): split backup page and sqlite-backup module          ← C3
53c93a7 fix: remove .ts import extension and deduplicate ensureCompanyFullName ← C2 fix
83173ae refactor(import-export): split page and utility into focused modules ← C2
90b845d refactor(employee): extract Form, Detail, InlineRow components      ← C1
149abf7 refactor(i18n): convert overview and employee type to i18n keys      ← B3
fd6269a refactor(employee): extract utils and convert to i18n keys            ← B2
d32d486 refactor(settings): extract utils and convert to i18n keys            ← B1
415642d refactor: extract shared E2E test helpers                             ← A2
c11b55f refactor: extract shared utility functions (format/error/type-guards) ← A1
e7c4480 docs: add P1 bugfix design and implementation plan                   ← 文档
a06c565 chore(release): finalize layoutfix stabilization and docs sync        ← main HEAD
```

**统计：** 110 files changed, +7500 insertions, -1897 deletions（含 dist-electron 构建产物）

---

## 十、注意事项

1. **Haiku 不可用** — 所有标注 Haiku 的任务用 Sonnet 替代（用户已批准）
2. **Sonnet 偶尔 503** — 若不可用，可用 Opus 替代（需用户批准）
3. **E2E 测试需要 GUI** — Claude 无法运行，需用户手动执行
4. **每个任务执行前** — 必须告知模型选择并获得用户批准
5. **subagent-driven development** — 每个任务都要走完整的 实现→spec 审查→质量审查 流程
6. **文件红线** — 所有 ts/tsx 文件 ≤ 200 行
7. **i18n 红线** — store 层零硬编码中文，所有用户可见文本通过 i18n
8. **推送到远程** — 尚未推送，所有变更仅在本地 bugfix 分支
9. **Code Quality 遗留 Minor** — C1 有 4 个 minor（updateDraft 防御检查、useCallback、冗余条件）；C3 有 3 个 minor（notice 类型重复、.bak 文件、缺 JSDoc）。均不阻塞，可在 G1 阶段统一清理

---

## 十一、本次会话总结（2026-02-17）

**使用模型：** Opus 4.6（主控） + Sonnet（subagent，Batch C 实现+审查）
**完成内容：** Batch C 全部 3 个任务（C1、C2、C3）
**执行流程：** 每个任务均完整走了 Implementer → Spec Review → Code Quality Review 三阶段
**关键成果：**
- 6 个超大文件全部拆分至 ≤200 行红线内
- 创建 10 个新模块（7 个组件 + 2 个 lib + 1 个 electron 模块）
- 删除 1 个旧文件（p1-employee-import-export.ts）
- 67/67 单元测试全部通过
- 4 个 commit（C1 + C2 + C2fix + C3）
